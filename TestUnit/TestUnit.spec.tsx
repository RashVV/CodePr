import React from 'react';
import { expect } from 'chai';
import Testunit from './TestUnit';

describe('Spec Testunit', function () {
    it('it exists', () => {
        expect(Testunit).to.be.ok;
    });
});
